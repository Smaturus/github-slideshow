# Стационарное УФПК для системы (2.5): асимптотика при малом ε

Режим **слабого шума и трения** (уравнения 2.5–2.7). Стационарное решение, разложение по ε², усреднение по быстрой переменной.

**Назначение текста:** черновик разделов статьи в стиле **ПММ** (прикладная постановка, явные формулы, ссылки на классические результаты без полного доказательства). Блок прикладной механической интерпретации для **МТТ** — отдельно, позже.

---

## 1. Обозначения

| Символ | Определение |
|--------|-------------|
| κ | D · A⁻¹ (связь из уравнений Эйлера) |
| α | φ̂ (нормированное трение) |
| ν | ν̂ (нормированная интенсивность шума) |
| σ₀ | √(ν · A⁻¹) |
| σ | ε · σ₀ |
| φ₀ | ε² · α |
| β | α · A · ν⁻¹ |

Малый параметр: **ε ∈ (0, 1]**.

**Полярные координаты:** ω₁ = r sin θ, ω₂ = r cos θ, r ≥ 0.

Отношение φ₀ · ν₀⁻¹ (масштаб средней энергии) **не зависит от ε**.

---

## 2. Система (2.5) в нормированных параметрах

```
dω₁ = (−κ·ω₁·ω₂ − ε²·α·ω₁) dt + ε·σ₀ dW₁
dω₂ = ( κ·ω₁²   − ε²·α·ω₂) dt + ε·σ₀ dW₂
```

- Шум **аддитивный** → исчисления Ито и Строноновича совпадают.
- При D = 0 (κ = 0): изотропный процесс Орнштейна–Уленбека.

---

## 3. Разложение стационарной плотности

```
p^ε(ω₁, ω₂) = (1 / Z(ε)) · [ p₀ + ε² · p₁ + ε⁴ · p₂ + O(ε⁶) ]
```

Члены с нечётными степенями ε отсутствуют (трение и диффузия входят как ε²).

**Z(ε) = 1 − ε² · κ · √(βπ) + O(ε⁴)** — нормировочная константа.

---

## 4. Ведущий член p₀ (гаусс при D = 0)

При κ = 0 стационарное распределение **точное** и **не зависит от ε**:

```
p₀(ω₁, ω₂) = (β / π) · exp( −β · (ω₁² + ω₂²) )
```

где **β = α·A / ν**.

Маргинальная плотность по ω₁:

```
ρ₀(ω₁) = √(β / π) · exp( −β · ω₁² )
```

Дисперсия: **E[ω₁²] = ν / (2·α·A)**.  
Средняя энергия: **E[E] = ν / (4·α·A)**, E = ½(ω₁² + ω₂²).

---

## 5. Стационарное УФПК

Дрейф:

```
b₁ = −κ·ω₁·ω₂ − ε²·α·ω₁
b₂ =  κ·ω₁²   − ε²·α·ω₂
```

Стационарное уравнение (нулевой ток):

```
∂/∂ω₁ (b₁ · p) + ∂/∂ω₂ (b₂ · p) = (ε² · σ₀² / 2) · Δp
```

Векторно: **∇·(b · p) = (ε² · σ₀² / 2) · Δp**.

Разделение дрейфа:

```
b = b_D + ε² · b_f

b_D = ( −κ·ω₁·ω₂ ,  κ·ω₁² )        — нелинейная связь
b_f = ( −α·ω₁     , −α·ω₂ )        — трение (Rayleigh)
```

---

## 6. Уравнение для первой поправки p₁ (порядок ε²)

Полное стационарное уравнение на порядке ε² (при **κ = O(1)**):

```
∇·(b_D · p₁) + ∇·(b_f · p₁) − (σ₀² / 2) · Δp₁ = κ · ω₂ · p₀
```

Эквивалентная форма через **p₁ = Q · p₀** (так как **b_D · ∇p₀ = 0**):

```
∇·(b_D · Q) − (σ₀² / 2) · ΔQ = κ · ω₂
```

---

## 7. Явное выражение для p₁

Обозначим **r = √(ω₁² + ω₂²)**. При **r > 0** частное решение:

```
p₁(ω₁, ω₂) = −κ · p₀(ω₁, ω₂) / r
```

**В декартовых координатах:**

```
p₁(ω₁, ω₂) = − κ · (β / π) · exp( −β · (ω₁² + ω₂²) ) / √(ω₁² + ω₂²)
```

**В полярных координатах:**

```
p₁(r, θ) = − κ · (β / π) · exp( −β · r² ) / r
```

Поправка **p₁ не зависит от угла θ**, только от радиуса **r**.

### Проверка

Подстановка **Q = −κ / r** в уравнение для **Q**:

- **∇·(b_D · Q) = κ · ω₂**
- **Δ(1/r) = 0** при **r > 0** в двумерном случае

### Нормировка полной плотности

Интеграл **∬ p₁ dω₁ dω₂ = −κ · √(βπ) ≠ 0**. Нормированная плотность:

```
p^ε = (1 / Z(ε)) · ( p₀ + ε² · p₁ + … )
```

```
Z(ε) = 1 − ε² · κ · √(βπ) + O(ε⁴)
```

### Замечание

Член **p₁ ~ −κ / r** сингулярен при **r → 0** — особенность формального ряда. Формула описывает основную массу при **r = O(1)**.

---

## 8. Полная плотность p в виде ряда (явно)

Общий вид (при **κ = O(1)**, **r > 0**):

```
p^ε = (1 / Z(ε)) · [ p₀ + ε² · p₁ + ε⁴ · p₂ + O(ε⁶) ]
```

### Декартовы координаты (ω₁, ω₂)

```
p^ε(ω₁, ω₂) = (1 / Z(ε)) · {
    (β/π) · exp( −β·(ω₁² + ω₂²) )
  − ε² · κ · (β/π) · exp( −β·(ω₁² + ω₂²) ) / √(ω₁² + ω₂²)
  + ε⁴ · p₂(ω₁, ω₂)
  + O(ε⁶)
}
```

**Компактная форма** (вынесен общий множитель):

```
p^ε(ω₁, ω₂) = (β / πZ(ε)) · exp( −β·(ω₁² + ω₂²) ) · [ 1 − ε²·κ / √(ω₁² + ω₂²) + ε⁴·Q₂(ω₁, ω₂) + O(ε⁶) ]
```

где **Q₂ = p₂ / p₀**.

### Полярные координаты (r, θ)

```
ω₁ = r · sin θ ,   ω₂ = r · cos θ ,   r = √(ω₁² + ω₂²) ,   r ≥ 0
```

```
p^ε(r, θ) = (1 / Z(ε)) · {
    (β/π) · exp( −β·r² )
  − ε² · κ · (β/π) · exp( −β·r² ) / r
  + ε⁴ · p₂(r, θ)
  + O(ε⁶)
}
```

**Компактная форма:**

```
p^ε(r, θ) = (β / πZ(ε)) · exp( −β·r² ) · [ 1 − ε²·κ / r + ε⁴·Q₂(r, θ) + O(ε⁶) ]
```

**p₀ и p₁ не зависят от θ**; угловая зависимость появляется в **p₂**.

### Явное выражение для p₂ (порядок ε⁴)

Уравнение для **p₂**:

```
∇·(b_D · p₂) + ∇·(b_f · p₂) − (σ₀² / 2) · Δp₂ = G(ω₁, ω₂) · p₀
```

```
G(ω₁, ω₂) = κ·α / r + κ²·ω₂ + κ·ν / (2·A·r³)     ,   r = √(ω₁² + ω₂²)
```

```
G(r, θ) = κ·α / r + κ²·r·cos(θ) + κ·ν / (2·A·r³)     ,   ω₂ = r·cos θ
```

**Явное решение (главные члены порядка ε⁴):**

```
p₂(ω₁, ω₂) = p₀(ω₁, ω₂) · [ κ² / (2·r²) + κ²·ω₂ / (2·α·r) ]
```

**В декартовых координатах:**

```
p₂(ω₁, ω₂) = (β / π) · exp( −β·(ω₁² + ω₂²) ) · [
    κ² / ( 2·(ω₁² + ω₂²) )
  + κ²·ω₂ / ( 2·α·(ω₁² + ω₂²)^(3/2) )
]
```

**В полярных координатах** (ω₁ = r sin θ, ω₂ = r cos θ):

```
p₂(r, θ) = (β / π) · exp( −β·r² ) · [ κ² / (2·r²) + κ²·cos(θ) / (2·α) ]
```

**Множитель Q₂ = p₂ / p₀:**

```
Q₂(r, θ) = κ² / (2·r²) + κ²·cos(θ) / (2·α)
```

---

### Полная плотность p^ε (подстановка p₀, p₁, p₂)

**Декартовы координаты:**

```
p^ε(ω₁, ω₂) = (1 / Z(ε)) · (β / π) · exp( −β·(ω₁² + ω₂²) ) · {
    1
  − ε² · κ / r
  + ε⁴ · [ κ² / (2·r²) + κ²·ω₂ / (2·α·r) ]
  + O(ε⁶)
}
```

```
r = √(ω₁² + ω₂²)
```

**Полярные координаты** (ω₁ = r sin θ, ω₂ = r cos θ):

```
p^ε(r, θ) = (1 / Z(ε)) · (β / π) · exp( −β·r² ) · {
    1
  − ε² · κ / r
  + ε⁴ · [ κ² / (2·r²) + κ²·cos(θ) / (2·α) ]
  + O(ε⁶)
}
```

**Развёрнутая форма (полярная):**

```
p^ε(r, θ) = (1 / Z(ε)) · {
    (β/π) · exp( −β·r² )
  − ε² · κ · (β/π) · exp( −β·r² ) / r
  + ε⁴ · (β/π) · exp( −β·r² ) · [ κ²/(2·r²) + κ²·cos(θ)/(2·α) ]
  + O(ε⁶)
}
```

### Нормировка

```
Z(ε) = 1 − ε² · κ · √(βπ) + O(ε⁴)
```

---

## 9. Маргинальная плотность: связное уравнение

```
p₁^marg(ω₁) = ∫ p₁(ω₁, ω₂) dω₂     (интеграл по всей оси ω₂)
```

Определим момент:

```
m(ω₁) = ∫ ω₂ · p₁(ω₁, ω₂) dω₂
```

После интегрирования уравнения для p₁ по ω₂:

```
κ·ω₁ · m(ω₁) = 2·α · ( β·ω₁² − ½ ) · ρ₀(ω₁)
```

Отсюда явно:

```
m(ω₁) = (2·α / (κ·ω₁)) · ( β·ω₁² − ½ ) · ρ₀(ω₁)     при ω₁ ≠ 0
```

Полная p₁^marg(ω₁) — чётная функция; восстанавливается из 2D-уравнения, а не только из m(ω₁).

---

## 10. Сводная таблица

| Величина | Формула |
|----------|---------|
| p^ε (ряд) | (1/Z(ε)) · [ p₀ + ε²·p₁ + ε⁴·p₂ + … ] |
| p^ε (компактно, полярн.) | (β/πZ(ε)) · exp(−β·r²) · [ 1 − ε²·κ/r + ε⁴·Q₂ + … ] |
| p₀ | (β/π) · exp(−β·(ω₁² + ω₂²)) |
| p₁ | −κ · p₀ / r |
| p₂ | p₀ · [ κ²/(2r²) + κ²·ω₂/(2α·r) ] |
| p₂ (полярн.) | p₀ · [ κ²/(2r²) + κ²·cos(θ)/(2α) ] |
| p^ε (полярн., до ε⁴) | (β/πZ) · exp(−βr²) · [ 1 − ε²κ/r + ε⁴(κ²/(2r²)+κ²cosθ/(2α)) ] |
| G (источник для p₂) | κ·α/r + κ²·ω₂ + κ·ν/(2·A·r³) |
| β | α·A / ν |
| ρ₀(ω₁) | √(β/π) · exp(−β·ω₁²) |
| Z(ε) | 1 − ε² · κ · √(βπ) + O(ε⁴) |
| Момент m(ω₁) | (2α/(κ·ω₁)) · (β·ω₁² − ½) · ρ₀(ω₁) |
| E[ω₁²] | ν / (2αA) — не зависит от ε |
| E[E] | ν / (4αA) — не зависит от ε |

---

## 11. Выбор метода (стационарный случай)

| Задача | Рекомендуемый подход | Раздел |
|--------|---------------------|--------|
| **p(r)**, **E[r²]**, глобальная картина при **κ = O(1)** | Усреднение по **θ** (Хасминский) | **§13–§14** |
| Локальная 2D-поправка при **r = O(1)** | Разложение **p₀ + ε²p₁ + …** | **§3, §7–§8** |
| **κ = 0** | Точный гаусс **p₀** | **§4** |
| Угловая структура при **Λ = O(1)** | **q(θ \| r)** из §14 | **§14** |
| Поправки **ε², ε⁴** к 2D-плотности | Чепмен–Энског | **§15** |

Для статьи в **ПММ** основной текст: **§13 → §14** (ведущее приближение и проверка моментов); **§15** — формальные поправки; **§16–§17** — сравнение методов и оговорки.

---

## 12. Полярные координаты

```
ω₁ = r · sin θ
ω₂ = r · cos θ
r² = ω₁² + ω₂² ,   r ≥ 0
```

Дрейф в полярных координатах:

```
b₁ = −κ · r² · sin θ · cos θ − ε² · α · r · sin θ
b₂ =  κ · r² · sin² θ   − ε² · α · r · cos θ
```

Ключевое свойство дрейфа:

```
ω₁·b₁ + ω₂·b₂ = −φ₀ · r² = −ε²·α · r²
```

Диссипация всегда направлена к меньшему r; члены с κ ортогональны радиусу при φ₀ = 0.

---

## 13. Усреднение по быстрой переменной (метод Хасминского)

При **κ = O(1)** и малом **ε** в системе (2.5) естественно выделить **быструю** угловую динамику и **медленную** радиальную. Трение и шум входят как **ε²** — медленный масштаб времени **τ = ε²t**.

Ниже: физическое разделение масштабов, **утверждение** об усреднении (со ссылкой на классическую теорию), **проверка применимости** к (2.5). Явный вывод формул — **§14**.

---

### 13.1. Быстрая и медленная переменные

**Полярные координаты** (§12): **ω₁ = r sin θ**, **ω₂ = r cos θ**, **r ≥ 0**.

| Переменная | Время релаксации | Источник в (2.5) |
|-----------|------------------|------------------|
| **θ** | **O(1)** | дрейф связи **κ** |
| **r** | **O(1/ε²)** | трение **ε²α**, шум **εσ₀** |
| **E = r²/2** | **O(1/ε²)** | **ω·b = −ε²α r²** (§12) |

Главные члены:

```
dθ/dt = κ · sin θ  +  O(ε/r)
dr/dt = −ε² · α · r  +  O(ε)
```

```
dE = −2·ε²·α·E  dt  +  ε²·σ₀²  dt  +  O(ε)  dW
```

**Физический смысл:** при **φ₀ = ε²α** члены с **κ** переносят «мгновенно» фазу **θ**; радиус и энергия эволюционируют медленно под действием трения и шума.

---

### 13.2. Оператор УФПК и разложение по ε

Стационарное уравнение **∇·(b·p) = (ε²σ₀²/2)Δp** (§5). Разделение дрейфа **b = b_D + ε²b_f** даёт разложение генератора:

```
L_ε p = L_0 p + ε² L_1 p
```

```
L_0 p = −∇·(b_D · p) + O(ε²)          — быстрый (угловой) оператор
L_1 p = −∇·(b_f · p) + (σ₀²/2) · Δp   — медленный (трение + диффузия)
```

```
b_D = ( −κ·ω₁·ω₂ , κ·ω₁² ) ,   b_f = ( −α·ω₁ , −α·ω₂ )
```

**Ключевое свойство:** **ω·b_D = 0** — дрейф **b_D** тангенциален окружности **r = const**; при фиксированном **r** быстрая динамика — только по **θ**.

*(В §6–§15 для разложения плотности используется эквивалентная adjoint-форма **L_ε^* = L_0^* + ε² L_1^***.)*

---

### 13.3. Утверждение об усреднении

**Источник:** теорема усреднения для систем с быстро осциллирующими переменными (Хасминский; см. также Freidlin–Wentzell; Pavliotis–Stuart, *Multiscale Methods*, гл. 17).

**Утверждение.** Пусть **κ, α, ν, A > 0** фиксированы, **κ = O(1)**, **ε** достаточно мало, **σ = εσ₀ > 0**. Тогда стационарная плотность **p^ε** процесса (2.5) на **ведущем порядке** факторизуется:

```
p^ε(r, θ) = p^(0)(r, θ)  +  O(ε²)     ,   r > 0
```

```
p^(0)(r, θ) = f_R(r) · q(θ | r) / r
            = (β/π) · exp( −β·r² ) · exp( −Λ(r)·cos θ ) / I₀( Λ(r) )
```

```
f_R(r) = 2·β·r·exp( −β·r² )                              — маргинальная по r (мера dr)
q(θ | r) = exp( −Λ(r)·cos θ ) / ( 2π·I₀( Λ(r) ) )        — условная по θ
Λ(r) = 2·κ·β·r² / ε² ,   β = α·A/ν
```

**Следствия (ведущий порядок):**

```
E[r²] = ν / (2·α·A) ,   E[E] = ν / (4·α·A) ,   E = ½(ω₁² + ω₂²)
```

```
Λ → 0  ⇒  q → 1/(2π)  ⇒  p^(0) → p₀ = (β/π) exp( −β·r² )     (гаусс, §4)
```

**В декартовых координатах:**

```
p^ε(ω₁, ω₂) = (β/π) · exp( −β·(ω₁² + ω₂²) ) · exp( −Λ·ω₂/r ) / I₀(Λ)  +  O(ε²)
```

```
r = √(ω₁² + ω₂²)
```

**Замечание (стиль ПММ).** Полное доказательство утверждения **не приводится** — оно следует из общей теории усреднения при выполнении условий §13.4. В статье достаточно **ссылки** и **прямого вывода** **p^(0)**, **f_R**, **q** (§14) с **проверкой моментов**.

**Что не входит в утверждение:** поправки **p₁**, **p₂** (§7–8, §15) — следующие порядки разложения Чепмена–Энскога.

---

### 13.4. Применимость к системе (2.5)

Проверка условий усреднения **для конкретной задачи** (вместо доказательства общей теоремы):

| № | Условие | Проверка для (2.5) |
|---|---------|-------------------|
| 1 | Выделены быстрая **θ** и медленная **r** | **dθ/dt = O(1)**, **dr/dt = O(ε²)** (§13.1) |
| 2 | Быстрая подсистема смешивает **θ** при **ε > 0** | угловая диффузия **D_θ = ε²ν/(2Ar²) > 0** (§14, шаг 2); при **ε = 0** — неподвижные точки **θ = 0, π** |
| 3 | Медленная подсистема имеет confining-распределение | трение **−ε²αω** → **p ~ exp(−βr²)** при **\|ω\| → ∞** |
| 4 | Усреднённый радиальный дрейф и диффузия | **b̄(r) = −αr**, **D̄ = σ₀²** (§14, шаг 3) |
| 5 | Маргинальная **f_R** и моменты | **f_R = 2βr exp(−βr²)**, **E[r²] = ν/(2αA)** — совпадает с режимом (2.7) |

**Вывод:** метод Хасминского **применим** к (2.5) в режиме (2.7); ведущее приближение **p^(0)** определяется явными формулами §14.

---

### 13.5. Сравнение с формальным разложением по ε

| | **p₀ + ε²p₁ + …** (§3, §8) | **Усреднение (§13–§14)** |
|---|---|---|
| **Ведущий член** | изотропный **p₀** | **p^(0) = p₀ · exp(−Λ cos θ)/I₀** |
| **Маргинальная p(r)** | через интегрирование ряда | **f_R = 2βr exp(−βr²)** — без **1/r** |
| **Область** | локально **r = O(1)** | глобально по **r ≥ 0** |
| **Поправки ε², ε⁴** | **p₁, p₂** (§7–8) | те же **p₁, p₂** (§15) |

Для **ПММ** основной результат — **§13.3 + §14**; разложение **p₀ + ε²p₁ + …** — альтернативное представление (§16).

---

### 13.6. Алгоритм (схема)

```
1. Выделить быструю θ и медленную r.
2. Найти q(θ | r) из стационарного уравнения L_0 на окружности.
3. Усреднить медленный оператор L_1 → b̄(r), D̄.
4. Решить 1D-УФПК для f_R(r).
5. Собрать p^(0)(r, θ) = f_R(r) · q(θ | r) / r.
6. Поправки ε², ε⁴ — разложение Чепмена–Энскога (§15).
```

**§14** — пошаговое выполнение пунктов 1–5.

---

## 14. Вывод ведущего приближения (алгоритм Хасминского)

**§14** — основной вычислительный блок для статьи в **ПММ**: прямой вывод **q(θ|r)**, **f_R(r)**, **p^(0)** из уравнения (2.5) и проверка **E[r²]**. Ссылается на утверждение §13.3.

### Шаг 1. Быстрая θ и медленная r

**Полярные координаты:** ω₁ = r sin θ, ω₂ = r cos θ, r ≥ 0.

Из (2.5) в полярной форме (главные члены):

```
b_r = −ε² · α · r                    — радиальный дрейф (только трение)
b_θ = κ · r² · sin θ                 — тангенциальный дрейф (связь κ)
```

```
dθ/dt = b_θ / r² = κ · sin θ         — O(1), быстрая переменная
dr/dt = b_r = −ε² · α · r            — O(ε²), медленная переменная
```

**Шум:** **σ = ε·σ₀**, изотропный → радиальная и угловая диффузия порядка **ε²**.

**Масштабы времени:**

| Переменная | τ |
|-----------|---|
| **θ** | **O(1)** |
| **r** | **O(1/ε²)** |
| **E = r²/2** | **O(1/ε²)** |

---

### Шаг 2. Угловая плотность q(θ | r)

**Стационарное уравнение на окружности** при фиксированном **r** (быстрый оператор **L_0**, §13.2):

```
L_0^* q = −(1/r) · ∂/∂θ ( b_θ · q ) + O(ε²)
```

На ведущем порядке — **1D-УФПК на окружности** (угол **θ**, параметр **r**):

```
∂/∂θ ( κ · sin θ · q  −  D_θ(r) · ∂q/∂θ ) = 0
```

**Угловая диффузия** (из изотропного шума **ε·σ₀** при фикс. **r**):

```
D_θ(r) = ε² · σ₀² / (2 · r²) = ε² · ν / (2 · A · r²)
```

**Решение** (нормировка **∫₀^{2π} q dθ = 1**):

```
q(θ | r) = (1 / Z(r)) · exp( −Λ(r) · cos θ )
```

```
Λ(r) = κ / D_θ(r) = 2 · κ · r² · A / (ε² · ν) = 2 · κ · β · r² / ε²
```

**Нормировочная константа** (модифицированная функция Бesselel):

```
Z(r) = ∫₀^{2π} exp( −Λ(r) · cos θ ) dθ = 2π · I₀( Λ(r) )
```

**Пределы:**

```
Λ → 0  (ε → 0 при фикс. r):   q(θ | r) → 1/(2π)     (равномерное)
Λ ≫ 1:                        q концентрируется у θ = 0  (cos θ = 1)
```

**O(ε²)-поправка к q:** учёт медленного члена в уравнении для **q** → **q = q₀ + ε² q₁ + …** (§15).

---

### Шаг 3. Усреднённые b̄(r) и D̄(r)

Усредняем медленный оператор **L_1** по **q(θ | r)**:

```
⟨f⟩_q = ∫₀^{2π} f(θ) · q(θ | r) dθ
```

**Радиальный дрейф** (из **b_f**):

```
e_r = ( sin θ , cos θ )     (единичный «радиальный» вектор)
b_f · e_r = −α · r · ( sin²θ + cos²θ ) = −α · r
```

```
b̄(r) = ⟨b_f · e_r⟩_q = −α · r          — не зависит от q
```

**Диффузия** (изотропная):

```
D̄(r) = σ₀² = ν / A                     — на ведущем порядке
```

**Усреднённый медленный SDE** (в медленном времени **τ = ε²·t**):

```
dr = −α · r · dτ  +  σ₀ · dW_τ
```

---

### Шаг 4. 1D-УФПК для f_R(r)

**Стационарное уравнение** для маргинальной плотности **f_R(r)** (мера **dr**):

```
−(d/dr)( α · r · f_R ) + (σ₀²/2) · (1/r) · (d/dr)( r · d f_R / dr ) = 0
```

**Решение:**

```
f_R(r) = 2 · β · r · exp( −β · r² ) ,   r ≥ 0 ,   β = α·A / ν
```

**Проверка моментов:**

```
E[r²] = ∫₀^∞ r² · f_R(r) dr = ν / (2·α·A)
E[E] = E[r²/2] = ν / (4·α·A)
```

---

### Шаг 5. Восстановление p^(0)(ω₁, ω₂)

**Итоговая 2D-плотность** (мера **r dr dθ**):

```
p(r, θ) = f_R(r) · q(θ | r) / r
```

**Подстановка** **f_R**:

```
p(r, θ) = 2 · β · exp( −β · r² ) · q(θ | r)
```

**Сравнение с p₀:**

```
p₀ = (β/π) · exp( −β · r² )
p(r, θ) = p₀(r) · 2π · q(θ | r)
```

**При равномерном q = 1/(2π):**

```
p(r, θ) = p₀(r) = (β/π) · exp( −β · r² )     — гаусс
```

**В декартовых координатах:**

```
p(ω₁, ω₂) = 2 · β · exp( −β · (ω₁² + ω₂²) ) · q( θ(ω₁, ω₂) | r ) / (2π)
```

```
r = √(ω₁² + ω₂²) ,   cos θ = ω₂ / r ,   sin θ = ω₁ / r
```

**Явная формула (результат §13.3):**

```
p^(0)(ω₁, ω₂) = (β/π) · exp( −β · r² ) · exp( −Λ(r) · cos θ ) / I₀( Λ(r) )
```

```
Λ(r) = 2 · κ · β · r² / ε²
```

**Проверка:** при **q = 1/(2π)** получаем **p^(0) = p₀** (§4). Маргинальная по **r** — **f_R** (шаг 4).

---

### Шаг 6. Поправки ε², ε⁴ — Чепмен–Энског

Поправки **p₁**, **p₂** к **p^(0)** — **§15**. Для **ПММ** ведущий результат — шаги 1–5; §15 — уточнение 2D-картины.

---

## 15. Поправки порядка ε² и ε⁴ (разложение Чепмена–Энскога)

**Статус для ПММ:** формальное асимптотическое разложение стационарного УФПК; явные **p₁**, **p₂** согласованы с уравнением на порядках **ε²**, **ε⁴**. **Сходимость ряда не утверждается** (§17).

**Связь с §13–§14:** **p^(0) = p₀·q(θ|r)** — ведущий член; **p₁**, **p₂** — поправки к полной 2D-плотности.

### 15.1. Разложение (согласованное с §3, §7, §8, §14)

```
p^ε(ω₁, ω₂) = p₀(r) · q(θ | r)  +  ε² · p₁(r, θ)  +  ε⁴ · p₂(r, θ)  +  O(ε⁶)
```

```
p₀(r) = (β / π) · exp( −β · r² )
q(θ | r) = exp( −Λ · cos θ ) / ( 2π · I₀(Λ) )     — §14, шаг 2
```

**Эквивалентная форма через множители** (как в §8):

```
p^ε = p₀(r) · [ q(θ | r)  +  ε² · Ψ₁(r, θ)  +  ε⁴ · Ψ₂(r, θ)  +  … ]
```

---

### 15.2. Иерархия по порядкам ε

Из **L_ε^* = L_0^* + ε² L_1^*** (§13.2) и разложения **p^ε = p^(0) + ε² p^(1) + ε⁴ p^(2) + …**:

| Порядок | Уравнение |
|---------|-----------|
| **O(1)** | **L_0^* p^(0) = 0** |
| **O(ε²)** | **L_0^* p^(1) = −L_1^* p^(0) − G(p^(0))** |
| **O(ε⁴)** | **L_0^* p^(2) = −L_1^* p^(1) − G(p^(1)) + ⟨…⟩** |

```
G(p) = ∇·(b_D · p)     — «несогласованность» гауссового p₀ с b_D
```

**Ключевой факт (§6–7):** **L_1^* p₀ = 0**, но **G(p₀) = κ · ω₂ · p₀ = κ · r · cos θ · p₀**.

---

### 15.3. Ведущий член p^(0)

```
p^(0)(r, θ) = p₀(r) · q(θ | r)
```

При **Λ → 0**: **q → 1/(2π)**, **p^(0) → p₀** (гаусс, §4).

---

### 15.4. Поправка O(ε²): явный p₁

**Анзац** (разделение радиальной и угловой частей):

```
p₁ = p₀(r) · [  Q_r(r)  +  Q_θ(θ | r)  ]
```

**Подстановка** **p₁ = p₀ · Q** в уравнение O(ε²):

```
∇·(b_D · p₁) + L_1^* p₁ = −G(p₀) = −κ · r · cos θ · p₀
```

При **b_D · ∇p₀ = 0** (§7):

```
p₀ · [ ∇·(b_D · Q) + L_1^* Q · p₀/p₀ ] = −κ · r · cos θ · p₀
```

**Частное решение по радиальной части** (§7, проверка §7):

```
Q_r(r) = −κ / r
```

```
∇·(b_D · Q_r) = κ · ω₂ = κ · r · cos θ     (при b_D с коэффициентом κ)
```

```
p₁^sing(r, θ) = −κ · p₀(r) / r              — §7, сингулярная часть
```

**Однородное (угловое) дополнение Q_θ** — решение:

```
L_0^* (p₀ · Q_θ) = 0 ,   ∫₀^{2π} Q_θ · q(θ|r) dθ = 0
```

На практике **Q_θ = O(κ²/ε²)** мало при **Λ ≪ 1**; **ведущий член p₁ — радиальный**.

**Итог O(ε²):**

```
p₁(r, θ) = −κ · p₀(r) / r  +  p₀(r) · Q_θ(θ | r)  +  O(κ²/ε²)
```

**В декартовых координатах** (§7):

```
p₁(ω₁, ω₂) = −κ · (β/π) · exp( −β·(ω₁² + ω₂²) ) / √(ω₁² + ω₂²)
```

**Связь с §14:** **p₁^sing ~ −κ/r** — локальная поправка Чепмена–Энскога; **q(θ|r)** из Хасминского — в **p^(0)**, не в **p₁**.

---

### 15.5. Поправка O(ε⁴): явный p₂ и связь с cos θ

**Уравнение O(ε⁴):**

```
∇·(b_D · p₂) + L_1^* p₂ = −∇·(b_D · p₁) − L_1^* p₁
```

**Правая часть** при **p₁ = −κ p₀/r** (§8, вычислено):

```
−∇·(b_D · p₁) − L_1^* p₁  =  p₀ · [ α·κ/r  +  κ²·r·cos θ  +  κ·ν/(2·A·r³) ]
```

**Анзац** (§8):

```
p₂ = p₀(r) · [  κ² / (2·r²)  +  κ² · cos θ / (2·α)  +  Q₂^c(r, θ)  ]
```

| Член | Источник | Зависимость от θ |
|------|----------|------------------|
| **κ²/(2r²)** | нелинейная итерация **Q_r = −κ/r**; **Q_r²/2** в **p = p₀(1 + ε²Q_r + …)** | нет |
| **κ² cos θ/(2α)** | баланс члена **κ² r cos θ** в правой части O(ε⁴) | **∝ cos θ = ω₂/r** |
| **Q₂^c** | однородное / нормировка | O(1) |

**Явные формулы §8 (полярные, ω₁ = r sin θ, ω₂ = r cos θ):**

```
p₂(r, θ) = (β/π) · exp( −β·r² ) · [ κ²/(2·r²)  +  κ²·cos(θ)/(2·α) ]
```

**В декартовых координатах:**

```
p₂(ω₁, ω₂) = (β/π) · exp( −β·(ω₁² + ω₂²) ) · [
    κ² / ( 2·(ω₁² + ω₂²) )
  + κ²·ω₂ / ( 2·α·(ω₁² + ω₂²)^(3/2) )
]
```

---

### 15.6. Единая формула (Хасминский + Чепмен–Энског)

```
p^ε(ω₁, ω₂) = p₀(r) · exp( −Λ · cos θ ) / I₀(Λ)
              − ε² · κ · p₀(r) / r
              + ε⁴ · p₀(r) · [ κ²/(2·r²) + κ²·cos(θ)/(2α) ]
              + O(ε⁶)
```

```
r = √(ω₁² + ω₂²) ,   cos θ = ω₂/r ,   Λ = 2κβr²/ε²
```

**Разделение ролей:**

| Объект | Масштаб | Метод | § |
|--------|---------|-------|---|
| **p₀ exp(−Λ cos θ)/I₀** | O(1) | Хасминский, **q(θ\|r)** | §14 |
| **−ε² κ p₀/r** | O(ε²) | Чепмен–Энског, **G(p₀)** | §7, §15.4 |
| **ε⁴ p₀ κ² cos θ/(2α)** | O(ε⁴) | Чепмен–Энског, угловая гармоника | §8, §15.5 |
| **ε⁴ p₀ κ²/(2r²)** | O(ε⁴) | Чепмен–Энског, **Q_r²/2** | §8, §15.5 |

---

### 15.7. Мультипликативная форма (связь p₁ и p₂)

```
p^ε ≈ p₀(r) · q(θ | r) · exp( ε² · Q₁ + ε⁴ · ( Q₂ + Q₁²/2 ) + … )
```

```
Q₁ = −κ / r
Q₂ + Q₁²/2  →  Q₂ + κ²/(2r²)   на уровне ε⁴
```

Угловой член **κ² cos θ/(2α)** не выводится из **Q₁** alone — он идёт из **L_1^* p₁** (связь **κ** и **α**).

---

### 15.8. Сводная таблица p₁, p₂

| | **p₁ (ε²)** | **p₂ (ε⁴)** |
|---|---|---|
| **§7–8 (декарт.)** | **−κ p₀/r** | **p₀[κ²/(2r²) + κ²ω₂/(2αr)]** |
| **§15 (полярн.)** | **−κ p₀/r** | **p₀[κ²/(2r²) + κ² cos θ/(2α)]** |
| **CE источник** | **G(p₀) = κω₂ p₀** | **−∇·(b_D p₁) − L_1 p₁** |
| **θ-зависимость** | только в **Q_θ** (малый) | **∝ cos θ** в ведущем угловом члене |

---

### Итоговая таблица (Хасминский + ε²)

| Величина | Формула |
|----------|---------|
| **q(θ \| r)** | exp(−Λ cos θ) / (2π·I₀(Λ)) |
| **Λ(r)** | 2κβr² / ε² |
| **f_R(r)** | 2βr exp(−βr²) |
| **p (ведущий)** | (β/π) exp(−βr²) · exp(−Λ cos θ) / I₀(Λ) |
| **p (Λ→0)** | p₀ = (β/π) exp(−βr²) |
| **E[r²]** | ν/(2αA) |

---

## 16. Сравнение двух представлений p^ε

**Для ПММ:** основное — **усреднение** (§13–§14); формальный ряд (§3, §8) — альтернатива при **Λ ≪ 1**, **r = O(1)**.

Обозначения общие:

```
r = √(ω₁² + ω₂²) ,   ω₁ = r sin θ ,   ω₂ = r cos θ ,   cos θ = ω₂/r
p₀ = (β/π) · exp( −β·r² ) ,   β = α·A/ν ,   Λ = 2κβr²/ε²
Z(ε) = 1 − ε²·κ·√(βπ) + O(ε⁴)
```

---

### 16.1. Два представления p^ε

| | **Формальный ряд (§3, §8)** | **Хасминский + CE (§14–§15)** |
|---|---|---|
| **Идея** | разложение **вокруг гаусса p₀** | **усреднение по θ** + локальные поправки CE |
| **O(1)** | **p₀** (изотропный гаусс) | **p₀ · exp(−Λ cos θ) / I₀(Λ)** |
| **O(ε²)** | **p₁ = −κ p₀/r** | **−ε² κ p₀/r** (то же) |
| **O(ε⁴)** | **p₂ = p₀[κ²/(2r²) + κ² cos θ/(2α)]** | **ε⁴ p₀[κ²/(2r²) + κ² cos θ/(2α)]** (то же) |
| **Нормировка** | деление на **Z(ε)** | в §15.6 явно **без Z(ε)**; глобальная норма из **f_R** |
| **Угол на O(1)** | нет | **q(θ\|r) ≠ const** при **Λ = O(1)** |

**Формальный ряд** — прямое разложение стационарного УФПК по **ε²** в декартовых **(ω₁, ω₂)**.

**Хасминский + CE** — сначала **2D → (r, θ)** с явной **q(θ\|r)**, затем те же **p₁, p₂** из иерархии CE.

---

### 16.2. Формальный ряд p^ε (декартовы координаты)

**Общий вид:**

```
p^ε_form(ω₁, ω₂) = (1 / Z(ε)) · [ p₀ + ε²·p₁ + ε⁴·p₂ + O(ε⁶) ]
```

**Явно** (§8, **r = √(ω₁² + ω₂²)**):

```
p^ε_form(ω₁, ω₂) = (1 / Z(ε)) · (β/π) · exp( −β·(ω₁² + ω₂²) ) · {
    1
  − ε² · κ / r
  + ε⁴ · [ κ²/(2·r²) + κ²·ω₂ / (2·α·r) ]
  + O(ε⁶)
}
```

| Член | Зависимость от **ω₁, ω₂** |
|------|---------------------------|
| **p₀** | только **r² = ω₁² + ω₂²** |
| **p₁** | только **r** (радиально-симметричный) |
| **p₂** | **r** и **ω₂** (через **cos θ = ω₂/r**) |

**Мультипликативная форма:**

```
p^ε_form = (β / πZ(ε)) · exp( −β·r² ) · [ 1 − ε²·κ/r + ε⁴·Q₂ + O(ε⁶) ]
Q₂ = κ²/(2·r²) + κ²·ω₂/(2·α·r)
```

---

### 16.3. Формальный ряд p^ε (полярные координаты)

```
p^ε_form(r, θ) = (1 / Z(ε)) · (β/π) · exp( −β·r² ) · {
    1
  − ε² · κ / r
  + ε⁴ · [ κ²/(2·r²) + κ²·cos(θ)/(2·α) ]
  + O(ε⁶)
}
```

**Развёрнуто:**

```
p^ε_form(r, θ) = (1/Z(ε)) · {
    (β/π)·exp(−β·r²)
  − ε²·κ·(β/π)·exp(−β·r²)/r
  + ε⁴·(β/π)·exp(−β·r²)·[κ²/(2·r²) + κ²·cos(θ)/(2·α)]
  + O(ε⁶)
}
```

На порядках **ε², ε⁴** угол **θ** входит **только в p₂** (член **∝ cos θ**).

---

### 16.4. Хасминский + CE (декартовы координаты)

**Сложение** (§15.6):

```
p^ε_KH+CE(ω₁, ω₂) = p₀(r) · exp( −Λ(r)·cos θ ) / I₀( Λ(r) )
                    − ε² · κ · p₀(r) / r
                    + ε⁴ · p₀(r) · [ κ²/(2·r²) + κ²·ω₂/(2·α·r) ]
                    + O(ε⁶)
```

**Подстановка** **r, cos θ = ω₂/r**:

```
p^ε_KH+CE(ω₁, ω₂) = (β/π) · exp( −β·r² ) · exp( −Λ·ω₂/r ) / I₀(Λ)
                    − ε²·κ·(β/π)·exp(−β·r²)/r
                    + ε⁴·(β/π)·exp(−β·r²)·[κ²/(2·r²) + κ²·ω₂/(2·α·r)]
                    + O(ε⁶)
```

**Мультипликативная форма** (§15.7):

```
p^ε_KH+CE ≈ p₀(r) · [ exp(−Λ cos θ)/I₀(Λ) − ε²·κ/r + ε⁴·(κ²/(2r²) + κ²·ω₂/(2α·r)) ]
```

---

### 16.5. Хасминский + CE (полярные координаты)

```
p^ε_KH+CE(r, θ) = (β/π) · exp( −β·r² ) · exp( −Λ(r)·cos θ ) / I₀( Λ(r) )
                  − ε² · κ · (β/π) · exp( −β·r² ) / r
                  + ε⁴ · (β/π) · exp( −β·r² ) · [ κ²/(2·r²) + κ²·cos(θ)/(2·α) ]
                  + O(ε⁶)
```

**Связь с шагами §14:**

```
p^ε_KH+CE = f_R(r)/r · q(θ|r)  −  ε²·p₁  +  ε⁴·p₂  +  O(ε⁶)
f_R(r) = 2βr exp(−βr²) ,   q(θ|r) = exp(−Λ cos θ)/(2π I₀(Λ))
f_R/r = 2β exp(−βr²) = 2π · p₀     →     f_R/r · q = p₀ · exp(−Λ cos θ)/I₀
```

---

### 16.6. Побайтовое сравнение (полярные)

| Порядок | **Формальный ряд** | **Хасминский + CE** | Совпадение? |
|---------|-------------------|---------------------|-------------|
| **O(1)** | **p₀ = (β/π) exp(−βr²)** | **p₀ · exp(−Λ cos θ)/I₀(Λ)** | **да**, если **Λ → 0** (**exp(−Λ cos θ)/I₀ → 1**) |
| **O(ε²)** | **−κ p₀/r** (÷ **Z(ε)**) | **−κ p₀/r** | **да** (разница **Z(ε) − 1 = O(ε²)**) |
| **O(ε⁴)** | **p₀[κ²/(2r²) + κ² cos θ/(2α)]** | то же | **да** |
| **Нормировка** | **1/Z(ε)** | без **Z** в §15.6 | поправка **O(ε²)** |

**Разложение Bessel при малом Λ:**

```
exp(−Λ cos θ)/I₀(Λ) = 1 − Λ·cos θ + O(Λ²)     ,   Λ = 2κβr²/ε²
```

Угловая неоднородность **O(1)** в **KH+CE** **не входит** в формальный ряд на уровне **p₀** — она «спрятана» в быстром масштабе **θ** и появляется в **q(θ\|r)**.

---

### 16.7. Побайтовое сравнение (декартовы)

Подставляем **r = √(ω₁² + ω₂²)**, **ω₂ = r cos θ**:

| | **p^ε_form(ω₁, ω₂)** | **p^ε_KH+CE(ω₁, ω₂)** |
|---|---|---|
| **O(1)** | **(β/π) exp(−β(ω₁²+ω₂²))** | **(β/π) exp(−βr²) · exp(−Λ·ω₂/r) / I₀(Λ)** |
| **O(ε²)** | **−ε² κ (β/π) exp(−βr²)/r** | **−ε² κ (β/π) exp(−βr²)/r** |
| **O(ε⁴)** | **ε⁴ (β/π) exp(−βr²)·[κ²/(2r²)+κ²ω₂/(2αr)]** | **ε⁴ (β/π) exp(−βr²)·[κ²/(2r²)+κ²ω₂/(2αr)]** |
| **Множитель** | **1/Z(ε)** | **1** (в §15.6) |

**Члены ε² и ε⁴ совпадают**; отличие на **O(1)** — угловой множитель **exp(−Λ·ω₂/r)/I₀(Λ)** против **1**.

---

### 16.8. Когда представления согласуются

**1. Предел ε → 0 при фикс. r = O(1):**

```
Λ = 2κβr²/ε² → ∞    (формально)
```

На практике **согласование** проверяют при **Λ ≪ 1** (малый **κ**, большой **ε**, или **r ≪ ε/√(2κβ)**):

```
p^ε_KH+CE → p₀ − ε²κp₀/r + ε⁴ p₀[κ²/(2r²)+κ² cos θ/(2α)]   ≈   p^ε_form / Z(ε)
```

**2. Маргинальная по r:**

Формальный ряд после интегрирования по **θ** (или по **ω₂**):

```
∫ p^ε_form · r dr dθ  →  f_R(r) = 2βr exp(−βr²)   на ведущем порядке
```

Хасминский **f_R** **точно** даёт **2βr exp(−βr²)** — совпадение **E[r²] = ν/(2αA)**.

**3. Область r = O(1), κ = O(1):**

Оба описания — **формальная асимптотика**; **p₁ ~ 1/r** сингулярен у **r = 0** в обоих случаях.

**4. Глобально по r:**

**KH+CE** предпочтительнее: **f_R(r)** корректен при **r ≥ 0** без **1/r** в ведущей маргинальной; **q(θ\|r)** даёт физичную угловую структуру при **Λ = O(1)**.

---

### 16.9. Сводная таблица (обе системы координат)

| Величина | **Формальный ряд** (декарт / поляр) | **KH + CE** (декарт / поляр) |
|----------|-------------------------------------|------------------------------|
| **p₀** | **(β/π) exp(−βr²)** | **(β/π) exp(−βr²) · exp(−Λ cos θ)/I₀(Λ)** |
| **p₁** | **−κ p₀/r** | **−κ p₀/r** |
| **p₂** | **p₀[κ²/(2r²) + κ²ω₂/(2αr)]** / **p₀[κ²/(2r²)+κ² cos θ/(2α)]** | то же |
| **Полная (усеч.)** | **(1/Z(ε))·[p₀+ε²p₁+ε⁴p₂]** | **p₀·exp(−Λ cos θ)/I₀ − ε²κp₀/r + ε⁴p₂** |
| **θ на O(1)** | нет | **exp(−Λ cos θ)/I₀** |
| **θ на O(ε⁴)** | **∝ cos θ** | **∝ cos θ** |
| **Сингулярность r=0** | **p₁ ~ 1/r** | **p₁ ~ 1/r** |
| **§ в файле** | §3, §7, §8 | §14, §15 |

---

### 16.10. Итог (для ПММ)

- **Основной результат статьи:** **p^(0)** из §13–§14 (**f_R**, **q**, **E[r²]**).
- **p₁, p₂** совпадают в обоих представлениях; различие — только в ведущем члене (**p₀** vs **p₀·exp(−Λ cos θ)/I₀**).
- **Усреднение** предпочтительнее для **p(r)** и **Λ = O(1)**; **формальный ряд** — при **Λ ≪ 1**, **r = O(1)**.
- Подробные рекомендации по структуре рукописи — **§17**.

---

## 17. Статус результатов и рекомендации для статьи

Раздел для переноса в рукопись **ПММ** / **МТТ**: что включать в основной текст, что — в приложение, что не требует доказательства.

---

### 17.1. Структура статьи (ПММ)

| Блок | Содержание | Раздел файла | Доказательство |
|------|------------|--------------|----------------|
| **1. Постановка** | Система (2.5), режим (2.7), обозначения | §1–§2, §12 | — |
| **2. Ведущее приближение** | Утверждение §13.3 + вывод §14 | **§13–§14** | ссылка на Хасминского |
| **3. Проверка** | **E[r²]**, **f_R**, сравнение с режимом (2.7) | §14, шаг 4 | прямой расчёт |
| **4. Поправки** | **p₁**, **p₂**, единая формула | §15 | формальный вывод |
| **5. Сравнение методов** | усреднение vs ряд | §16 | обсуждение |

**Не включать в ПММ:** полное доказательство теоремы усреднения; оценки в нормах **L¹**, **L²**; Fredholm-теорию; внутренний слой у **r = 0**.

---

### 17.2. Что обосновано ссылкой, что — выводом

| Результат | Статус | Как оформить в статье |
|-----------|--------|----------------------|
| **p^(0) = p₀·exp(−Λ cos θ)/I₀** | теория усреднения + вывод §14 | «следует из … [Хасминский], явно получаем …» |
| **f_R = 2βr exp(−βr²)** | прямой вывод 1D-УФПК | уравнение + решение |
| **E[r²] = ν/(2αA)** | проверка | подстановка, сравнение с (2.7) |
| **p₁ = −κp₀/r** | формальный CE-вывод | «на порядке ε² из УФПК …» |
| **p₂ ~ κ²/(2r²) + κ² cos θ/(2α)** | формальный CE-вывод | то же |
| **Сходимость ряда** | **не утверждается** | «асимптотическое разложение» |

---

### 17.3. Оговорки (кратко для статьи)

1. **Точность ведущего члена** — **O(ε²)**; поправка **O(ε²)**, а не **O(ε)**, т.к. трение и шум масштабируются как **ε²** (§13.1).

2. **Область r = 0:** полярные координаты вырождены; **f_R(r)** регулярна при **r = 0**; поправка **p₁ ~ 1/r** — особенность формального ряда, не маргинальной **p(r)**.

3. **ε = 0:** без шума угол **θ** не смешивается; усреднение требует **σ = εσ₀ > 0**.

4. **Формальный ряд** (§3, §8) и **усреднение** (§13–§14) согласуются при **Λ ≪ 1** (§16).

---

### 17.4. Литература (минимальный набор для ПММ)

1. **Хасминский Р.З.** — принцип усреднения (классическая ссылка для отечественной школы).
2. **Freidlin M., Wentzell A.** — *Random Perturbations of Dynamical Systems* (усреднение SDE).
3. При необходимости: **Pavliotis G., Stuart A.** — *Multiscale Methods* (гл. 17).

---

### 17.5. Заготовка для МТТ (добавить позже)

*[Блок для журнала **МТТ** — прикладная механическая интерпретация: связь **κ** с уравнениями Эйлера, физический смысл **q(θ|r)** при **Λ = O(1)**, интерпретация **E[r²]** как средней энергии колебаний, численная иллюстрация.]*

---

*Файл: вывод для системы (2.5), режим (2.7). Полярные координаты: ω₁ = r sin θ, ω₂ = r cos θ. Стиль: **ПММ** (§13–§17); блок **МТТ** — §17.5.*
